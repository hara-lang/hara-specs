# Substrate transport

This draft separates portable substrate frames from their physical carrier. It
defines transport lifecycle and attachment once, then profiles direct, memory,
HTA/1, structured-clone, transferable, and WebSocket carriers.

Normative data is in `substrate-transport.edn`; the shared profile matrix is in
`conformance/transport.edn`.
